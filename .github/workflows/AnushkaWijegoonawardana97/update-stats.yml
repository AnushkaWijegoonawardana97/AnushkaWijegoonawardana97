name: Update stats

on:
  schedule:
    # Runs at 12am UTC
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  update-readme:
    name: Update Readme with Metrics
    runs-on: ubuntu-latest
    steps:
      - uses: athul/waka-readme@master
        with:
          WAKATIME_API_KEY: ${{ secrets.0db0cef4-3243-4673-bcbf-95ae9e70b8c4 }}
          GH_TOKEN: ${{ secrets.GH_TOKEN}}
          USERNAME: WinterWolf97
          SHOW_TITLE: false
